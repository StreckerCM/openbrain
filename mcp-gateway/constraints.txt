# Full resolved dependency set for mcp-gateway, transitives included.
#
# requirements.txt states WHAT this service depends on; this file states
# WHICH versions the build resolved to. Direct pins alone don't protect
# against a transitive major bump — pydantic or anyio could still break
# mcp 1.27.0 on a rebuild, which is the same failure mode that took the
# gateway down on 2026-08-11 (mcp 2.0.0 removed mcp.server.fastmcp).
#
# Captured 2026-08-12 from the running, verified container:
#   docker compose exec mcp-gateway pip freeze
#
# Regenerate the same way after any deliberate version bump. The freeze
# must come from the python:3.12-slim build image, not a dev machine —
# a different platform or Python version resolves a different set.
#
# Note: extras are illegal in constraints files (`mcp[http]==1.27.0`
# errors). Bare names only, which is what pip freeze emits.
annotated-types==0.8.0
anyio==4.14.2
asyncpg==0.31.0
attrs==26.1.0
certifi==2026.7.22
cffi==2.1.1
click==8.4.2
cryptography==50.0.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
httpx-sse==0.4.3
idna==3.18
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
mcp==1.27.0
pycparser==3.0
pydantic==2.13.4
pydantic-settings==2.15.0
pydantic_core==2.46.4
PyJWT==2.13.0
python-dotenv==1.2.2
python-multipart==0.0.32
referencing==0.37.0
rpds-py==2026.6.3
sentry-sdk==2.58.0
sse-starlette==3.4.8
starlette==1.0.0
typing-inspection==0.4.3
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn==0.46.0
